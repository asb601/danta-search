{
  "version": "2.0",
  "logging": {
    "applicationInsights": {
      "samplingSettings": {
        "isEnabled": true,
        "excludedTypes": "Request"
      }
    }
  },
  "extensionBundle": {
    "id": "Microsoft.Azure.Functions.ExtensionBundle",
    "version": "[4.*, 5.0.0)"
  },
  "extensions": {
    "eventGrid": {}
  },
  "concurrency": {
    "_comment": "Dynamic concurrency lets the host learn the optimal per-instance parallelism for the IO-bound enrich function instead of a fixed guess. snapshotPersistenceEnabled remembers it across restarts.",
    "dynamicConcurrencyEnabled": true,
    "snapshotPersistenceEnabled": true
  }
}
